services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: pathora
      POSTGRES_PASSWORD: pathora
      POSTGRES_DB: pathora
    ports:
      - "5432:5432"
    volumes:
      - pathora_postgres_data:/var/lib/postgresql/data

volumes:
  pathora_postgres_data:
