services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: pauta
      POSTGRES_PASSWORD: pauta
      POSTGRES_DB: pauta
    ports:
      - "5432:5432"
    volumes:
      - pauta_postgres_data:/var/lib/postgresql/data

volumes:
  pauta_postgres_data:
