# Local Postgres (docker-compose). En producción, sustituir por la
# connection string de Vercel Postgres.
DATABASE_URL=postgresql://pauta:pauta@localhost:5432/pauta

# Pagos
STRIPE_SECRET_KEY=

# Proveedor LLM (por decidir)
LLM_API_KEY=
